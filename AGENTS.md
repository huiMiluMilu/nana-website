# 项目协作约定

## 操作与部署日志

- 每次修改本项目的代码、样式、文案、配置或素材，都必须同步更新根目录的 `PROJECT_CHANGELOG.md`。
- 每次预览、构建、测试、部署、发布、回滚、服务恢复或 Git 推送，也必须记录时间、结果和状态。
- 日志必须区分本地预览、GitHub 源码推送、测试环境部署和生产环境部署，不得把未验证的操作写成成功。
- 日志与对应项目改动放入同一次 Git 提交，并推送到 `origin/main`。
