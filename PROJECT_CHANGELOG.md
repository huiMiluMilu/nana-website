# NaNa 课程网站项目更新日志

最后更新：2026-07-20 21:24（Asia/Shanghai）

这份文档记录本项目的功能操作、素材变更、验证结果、Git 保存和部署情况。历史内容依据当前项目对话与 Git 提交记录整理；首次 Git 提交以前的操作没有独立提交号，因此按开发阶段归纳。

## 日志维护规则

- 每次修改代码、样式、文案、配置或素材后，都要在本文件追加一条记录。
- 每次启动预览、执行构建、自动化测试、部署、发布、回滚或恢复服务后，都要记录结果。
- 日志应说明时间、目标、实际改动、涉及文件、验证方式、部署状态和 Git 提交。
- 未经验证的操作不能写成“成功”；GitHub 源码推送不能写成“生产环境已部署”。
- 日志与对应改动放在同一次 Git 提交中，并推送到 `origin/main`。
- 时间统一使用 `Asia/Shanghai`。

## 当前项目状态

- 项目：NaNa 视听艺术课程网站
- 技术：React、Vite、OGL、Remotion
- 当前分支：`main`
- GitHub：`https://github.com/huiMiluMilu/nana-website.git`
- 本地开发预览：`http://localhost:5173/`（2026-07-20 21:24 检查为 HTTP 200）
- 最新已记录功能提交：`Prepare stable public site hosting`（本次提交）
- 生产环境：尚无正式部署记录

## 历史操作记录

### 首次 Git 提交前：设计探索与交互原型

- 参考用户提供的页面截图和两段录屏，确认目标不是 React Bits 的 Card Swap，而是上下翻转、主卡背后不堆叠内容的纵向卡片队列。
- 调研 Motion Sites 等动效参考，并使用用户提供的 3D cylinder carousel 提示词作为卡片运动基础。
- 使用 React Bits Soft Aurora 思路和 OGL shader 创建黑色背景上的粉、紫、蓝动态光带。
- 建立第一页英文标题 `NANA AUDIOVISUAL ART CREATION`、顶部导航、辅助线和课程卡片区域。
- 删除卡片上的数字、文字、磁卡芯片等装饰，课程内容改为 GIF/动态图占位。
- 建立卡片悬停标题、点击主卡打开课程、悬停上下卡自动居中、滚轮驱动和 3 秒自动轮播。
- 卡片数量采用有限可见窗口，页面只显示约 6 张附近卡片，但可循环访问全部课程。
- 处理非全屏缩放、卡片圆角闪烁、背面内容、边缘细线和标题标签换行。
- 将首页介绍文案更新为：`Explore the relationship between what you see and what you hear. Build your own art worlds.`

### 2026-07-13：Git 提交与功能演进

以下记录按 Git 提交链顺序排列，时间取自各提交。

| 时间 | Git | 操作与结果 |
| --- | --- | --- |
| 11:17 | `28314de` | 建立 NaNa 网站源码，加入 React/Vite/OGL 页面、课程卡片组件、Soft Aurora 和 Remotion 动效源码。 |
| 11:17 | `6a83a1f` | 加入 15 节课程的调色板、静帧和 MP4 预览素材。 |
| 11:19 | `0cda97f` | 加入第 1、2 节课程 GIF。 |
| 11:19 | `44861ca` | 加入第 3、4 节课程 GIF。 |
| 11:21 | `5704d3f` | 加入第 5 至 8 节课程 GIF。 |
| 11:22 | `850ed7f` | 加入第 9 至 12 节课程 GIF。 |
| 11:23 | `07cbfb6` | 加入第 13 至 15 节课程 GIF，完成全部课程动态图素材接入。 |
| 11:08 | `6ec2a7d` | 更新第 15 节“无穷鸡腿视频广告工作流”视觉素材和 Remotion 动效。该提交时间早于前序提交，但位于其后的 Git 提交链。 |
| 11:10 | `c5bb056` | 将卡片改为分阶段的纵向翻转，减少主卡背后堆积。 |
| 11:13 | `5aab86c` | 按课程表匹配日期与课程主题，并调整左侧悬停标签样式。 |
| 11:16 | `b569a92` | 为课程卡片匹配真实学习页面链接。 |
| 11:22 | `d7b4eaf` | 课程链接改为新标签页打开，保留课程主页标签页。 |
| 11:27 | `c2e5891` | 删除卡片背面内容，避免翻转后出现重复画面。 |
| 11:58 | `d06d87e` | 调整连续卡片翻转的缓动和运动计算，使翻转更顺滑。 |
| 14:42 | `fefcf49` | 分离可见卡片，避免其他卡片在主卡背后形成堆叠。 |
| 15:38 | `1817da4` | 改进课程标签响应式布局、自动换行和悬停命中，并更新首页英文介绍文案。 |
| 16:09 | `7e57442` | 加入第一页滚动视差；新增羊皮纸撕边第二页、小红书作品卡片、横向拖动轮播和 8 张本地作品封面。 |
| 17:00 | `08cf8a3` | 替换风变横版 Logo，删除 `01—15`，将位置文案改为 `Soulful Poetic`；统一第二页标题字体并增加分层视差。 |
| 18:01 | `9951483` | 修正课程卡片的空间方向和课程顺序：向上滑时所有可见卡片向上、下一节从下方进入；卡片区拦截页面滚动，左侧和中间区域保留正常页面滚动。构建及浏览器交互测试通过。 |

## 运行与部署记录

### 2026-07-20 21:24

- 类型：公网预览托管准备与本地服务恢复。
- 目标：生成可供他人直接访问、且不依赖本地电脑持续运行的稳定预览链接。
- 操作：恢复 5173 端口 Vite 预览并验证 HTTP 200；尝试 Cloudflare Quick Tunnel，因当前网络阻断 QUIC 与 HTTP/2 边缘连接而停止，未将不可用的临时地址交付。随后创建正式托管站点，并为现有 Vite 静态站补充托管配置与静态资源 Worker 输出。
- 涉及文件：`.openai/hosting.json`、`hosting/worker.js`、`vite.config.js`。
- 验证：`npm run build` 通过，生成 `dist/client/index.html` 与 `dist/server/index.js`；托管发布尚在进行中，不在此条记录中标记为成功。
- 部署状态：本地预览已恢复；临时隧道失败并已关闭；正式托管待发布验证。
- Git：`Prepare stable public site hosting`（本次提交并推送至 `origin/main`）。

### 2026-07-19 18:10

- 类型：第二页视觉改版与页面分隔动效。
- 目标：删除羊皮纸效果，将第二页改为黑色背景，并用新的动态元素明确分隔第一页与第二页。
- 改动：移除羊皮纸背景、撕边裁切和纸张纤维；新增横跨页面的彩色光缝、移动光点与循环档案文字。第二页改为黑色影像档案风格，标题使用实心与描边组合，加入粉蓝视差光晕、旋转档案章、暗态卡片和当前作品荧光亮起效果。
- 涉及文件：`src/components/WorksShowcase.jsx`、`src/components/WorksShowcase.css`。
- 验证：`npm run build` 通过；重新启动本地 Vite 服务，`http://localhost:5173/` 返回 HTTP 200；1440×900 桌面与 390×844 手机浏览器截图确认分隔线、黑色背景、标题和卡片正常，无页面横向溢出；代码检索确认旧羊皮纸类名、变量、颜色和撕边裁切已移除。
- 部署状态：已更新本地开发预览；尚未部署测试环境或生产环境。
- Git：`Redesign works page as dark archive`（本次提交并推送至 `origin/main`）。

### 2026-07-17 15:55

- 类型：本地预览服务恢复。
- 目标：恢复 `http://localhost:5173/` 预览。
- 原因：5173 端口未监听，Vite 开发服务已退出；未修改页面代码。
- 操作：执行 `npm run dev -- --host 0.0.0.0 --port 5173`。
- 验证：`http://localhost:5173/` 返回 HTTP 200。
- 部署状态：仅恢复本地开发预览，不属于测试环境或生产环境部署。
- Git：`Log local preview service recovery`（本次提交并推送至 `origin/main`）。

### 2026-07-17 11:07

- 类型：首页交互引导样式与响应式修复。
- 目标：更换向下箭头样式，并确保浏览器缩放、不同窗口尺寸下始终贴近浏览器底部边缘。
- 改动：将箭头改为荧光黄细线圆形引导按钮；从首屏文档定位改为视口固定定位，使用安全区域底边距；首屏向下离开时自动淡出并停止响应点击。
- 涉及文件：`src/App.jsx`、`src/styles.css`。
- 验证：`http://localhost:5173/` 返回 HTTP 200；`npm run build` 通过；1440×900 与 1000×700 浏览器截图均确认按钮固定于底部边缘；点击后 URL 定位至 `#works`、页面向下滚动，按钮透明度为 0 且不再接收点击。
- 部署状态：仅更新本地开发预览；尚未部署测试环境或生产环境。
- Git：`Keep scroll arrow on viewport edge`（本次提交并推送至 `origin/main`）。

### 2026-07-17 11:03

- 类型：首页交互引导。
- 目标：在首屏靠近浏览器底部的位置增加与页面视觉一致的向下箭头。
- 改动：新增链接至第二页作品区的细线箭头，使用现有荧光黄、微光和低幅度下移节奏；保留底部三组页面信息文字。
- 涉及文件：`src/App.jsx`、`src/styles.css`。
- 验证：本地 Vite 预览服务已重新启动，`http://localhost:5173/` 返回 HTTP 200；`npm run build` 通过；1440×900 浏览器截图确认箭头位于首屏底部中央，且未遮挡底部信息文字。
- 部署状态：仅更新本地开发预览；尚未部署测试环境或生产环境。
- Git：`Add first-page scroll arrow`（本次提交并推送至 `origin/main`）。

### 2026-07-16 20:32

- 类型：首页视觉层次与第二页入口修复。
- 目标：扩大首屏粉紫蓝极光色带的可见范围和左侧文字下方亮度；将左侧标题上移以露出色带；确保羊皮纸只在向下进入第二页时出现。
- 改动：扩大 Aurora 画布至首屏边界外并提升饱和度、亮度；降低左侧遮罩在下方的遮蔽程度；在桌面端将左侧内容组上移。第二页羊皮纸改为自身顶部裁切的撕边，移除原先向首屏上方延伸的伪元素。
- 涉及文件：`src/styles.css`、`src/components/WorksShowcase.css`。
- 验证：`http://localhost:5173/` 返回 HTTP 200；`npm run build` 通过；使用 1440×900 浏览器截图检查首屏，羊皮纸未提前出现，左侧标题下方色带清晰可见；下滑截图确认第二页显示羊皮纸内容。浏览器页面错误为 0。
- 部署状态：已更新本地开发预览；尚未部署测试环境或生产环境。
- Git：`Refine hero aurora and parchment entry`（本次提交并推送至 `origin/main`）。

### 2026-07-15 17:46

- 类型：本地预览服务恢复。
- 目标：恢复 `http://localhost:5173/` 预览。
- 原因：5173 端口没有监听进程，Vite 开发服务已退出；未修改页面代码。
- 操作：执行 `npm run dev -- --host 0.0.0.0 --port 5173`。
- 验证：HTTP 请求返回 200，Node/Vite 进程在 `*:5173` 正常监听。
- 部署状态：仅恢复本地开发预览，不属于测试环境或生产环境部署。
- Git：`Log local preview service recovery`。

### 2026-07-15 17:06

- 类型：5 张卡片边缘频闪修复。
- 目标：保留所有卡片的绿色细线边缘，同时消除翻转和倾斜时的双线、跳动与亮度频闪。
- 原因：每张卡同时使用独立绿色厚度底板、前移 2.2px 的正面和实体 border；这些重叠 3D 平面在接近水平的透视角度下发生亚像素竞争。
- 改动：移除重复的 3D 底板几何，但不删除视觉边缘；每张卡改为在卡面最上层的 `::after` 固定描边，描边与卡面共用同一 3D 平面，视频无法覆盖它。悬停时描边仍会变亮。
- 涉及文件：`src/components/CylinderCarousel.jsx`、`src/components/CylinderCarousel.css`。
- 验证：全屏 5 张可见卡片全部存在描边，旧重叠底板数量为 0；翻转前、过程中、结束后均保持 5 张，所有视频无错误，Playwright 无页面错误，`npm run build` 通过。
- 服务：重新启动 `http://localhost:5173/`，HTTP 检查返回 200。
- 部署状态：已更新本地开发预览；尚未部署测试环境或生产环境。
- Git：`Stabilize card outline rendering`。

### 2026-07-15 16:55

- 类型：桌面卡片可见数量修复。
- 目标：恢复原版全屏首页可见 5 张卡片的层级，同时保留窄屏自适应。
- 原因：此前第二层上下边缘卡片在偏移超过 1.58 后被淡出并移到画面外，因此全屏只剩主卡和上下相邻卡，共 3 张。
- 改动：`src/components/CylinderCarousel.jsx` 恢复原提示词中的两段透视运动；桌面宽度不小于 1200px 且卡片舞台高度不小于 600px 时，显示主卡、上下相邻卡和上下边缘窥视卡，共 5 张；较窄视口继续显示 3 张。
- 验证：1920×1080 与 1440×900 均显示 5 张，1000×800 显示 3 张且无横向溢出；滚轮翻动前、过程中和结束后均保持 5 张，并按顺序进入下一节课程。Playwright 无页面错误，`npm run build` 通过。
- 服务：`http://localhost:5173/` 返回 HTTP 200。
- 部署状态：已更新本地开发预览；尚未部署测试环境或生产环境。
- Git：`Restore five-card desktop carousel`。

### 2026-07-15 15:27

- 类型：本地预览服务恢复。
- 目标：解决 `http://localhost:5173/` 无法打开的问题。
- 原因：5173 端口没有监听进程，Vite 开发服务已退出；未发现页面代码变更或代码错误。
- 操作：执行 `npm run dev -- --host 0.0.0.0 --port 5173`，重新启动固定端口预览。
- 验证：HTTP 请求返回 200，Node/Vite 进程在 `*:5173` 正常监听。
- 部署状态：仅恢复本地开发预览，不属于测试环境或生产环境部署。
- Git：`Log local preview service recovery`。

### 2026-07-14 16:35

- 类型：课程媒体稳定性修复。
- 目标：解决本地页面运行较长时间后，部分课程 GIF 变空白或停止显示的问题。
- 原因：15 个 GIF 文件合计约 69MB，轮播后会陆续进入浏览器并产生远高于压缩文件体积的帧解码内存；项目已有相同内容的 H.264 MP4，合计约 13MB。
- 改动：`src/components/CylinderCarousel.jsx` 改用 15 个现有 MP4；媒体元素由 GIF `<img>` 改为静音、自动播放、循环、行内播放的 `<video>`。卡片布局、翻转、标题和课程链接不变。
- 验证：15 个 MP4 均为 H.264、1280×800、约 5.056 秒；`npm run build` 通过，生产包不再输出课程 GIF；Playwright 完整访问 15 门课程，检查到 7 个视频、0 个 GIF、0 个损坏媒体，所有活动视频持续播放且无错误。额外等待超过一个循环后，解码帧从 49 增至 234。
- 服务：测试期间临时使用 5174 端口；测试后停止临时服务，并在 5173 端口重新启动 Vite，HTTP 检查返回 200。
- 部署状态：已恢复本地开发预览；尚未部署测试环境或生产环境。
- Git：`Replace course GIFs with looping videos`。

### 2026-07-13 21:18

- 类型：本地预览服务恢复。
- 目标：核实更新日志中的本地预览状态。
- 操作：首次检查 `localhost:5173` 返回连接失败；重新执行 `npm run dev -- --host 0.0.0.0`。
- 验证：再次请求 `http://localhost:5173/`，返回 HTTP 200。
- 部署状态：仅恢复本地开发预览，不属于测试环境或生产环境部署。
- Git：随 `Create project operation changelog` 一并记录。

### 2026-07-13 21:16

- 类型：项目文档与维护规则。
- 目标：建立长期更新日志，并补录此前操作、验证、Git 和部署状态。
- 改动：新增 `PROJECT_CHANGELOG.md`；新增项目级 `AGENTS.md`，要求后续每次操作和部署同步维护日志。
- 验证：对照完整 Git 提交链检查历史记录，并执行 Git diff 格式检查。
- 部署状态：文档将随本次提交推送到 GitHub `origin/main`；不涉及生产网站部署。
- Git：`Create project operation changelog`。

### 2026-07-13

- 将项目源码推送到 `huiMiluMilu/nana-website` 的 `main` 分支，并持续推送后续功能提交。
- 多次执行 `npm run build`，最近一次在 `9951483` 提交前通过 Vite 生产构建。
- 使用 Playwright/Chrome 验证课程卡片方向、课程顺序、滚轮区域隔离、响应式布局、Logo 尺寸和第二页视差。
- 本地 Vite 服务曾退出并出现 `localhost` 拒绝连接；已重新执行 `npm run dev -- --host 0.0.0.0`，恢复 `http://localhost:5173/` 预览。
- 尚未执行 Vercel、Netlify、GitHub Pages 或独立服务器等生产环境部署。

## 后续日志模板

复制以下模板并追加到“运行与部署记录”上方或对应日期下：

```markdown
### YYYY-MM-DD HH:mm

- 类型：功能 / 修复 / 素材 / 配置 / 测试 / 部署 / 回滚
- 目标：
- 改动：
- 涉及文件：
- 验证：
- 部署状态：未部署 / 本地预览 / 测试环境 / 生产环境
- Git：提交号与提交标题
```
